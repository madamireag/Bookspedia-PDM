<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Echipa07.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:Echipa07">

    <FlyoutItem Title="About us">
        <ShellContent
            ContentTemplate="{DataTemplate local:AboutPage}"
            Route="AboutPage" />
    </FlyoutItem>
    <FlyoutItem Title="Bookspedia">
        <ShellContent
            ContentTemplate="{DataTemplate local:MainPage}"
            Route="MainPage" />
    </FlyoutItem>
    <FlyoutItem Title="Search books by ...">
        <ShellContent
            ContentTemplate="{DataTemplate local:SearchBookPage}"
            Route="SearchBookPage" />
    </FlyoutItem>
    <FlyoutItem Title="Contact us">
        <ShellContent
            ContentTemplate="{DataTemplate local:ContactPage}"
            Route="ContactPage" />
    </FlyoutItem>

</Shell>
